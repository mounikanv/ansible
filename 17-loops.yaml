- name: loops
  hosts: local
  connection: local
  tasks:
  - name: Print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #item is a reserved key word
  loops:
  - Mounika
  - Ranjith
  - mouni    